#pragma once
#include <vector>

using namespace std;

// the .h file only contains the function delcaration
// then create a .cpp file (could also be the same name)
// .cpp file contains the definition
int SumOfVectors(vector<int>& to_sum);